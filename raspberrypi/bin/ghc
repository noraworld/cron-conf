#!/bin/sh

PPLOGGER_COMMAND=$(basename "$0")
PPLOGGER_GRANULARITY="daily"

main() {
  pplogger "$PPLOGGER_COMMAND" "$PPLOGGER_GRANULARITY" "\"command\": \"$(echo "$GHC_FROM" | sed 's/[[:space:]]*$//')\""
  gh "$@"
}

main "$@"
